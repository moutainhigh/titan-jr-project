<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.titanjr.fop.dao.TitanAccountDao">


    <select id="queryAccountBalanceList" parameterType="com.fangcang.titanjr.dto.request.AccountBalanceRequest"
            resultType="com.fangcang.titanjr.dto.bean.AccountBalance">

        SELECT
        userid,
        productid,
        initusablelimit as balanceusable,
        initfrozonamount as balancefrozon,
        initsettleamount as balancesettle,
        initcreditamount as balancecredit,
        initoverlimit as balanceoverlimit,
        inittotalamount as amount
        FROM
        t_tfs_balanceinfo
        <where>
            <if test="userid!=null">and userid = #{userid}</if>
        </where>
    </select>

</mapper>